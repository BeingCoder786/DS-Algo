fun main() {
    val grid = arrayOf(
        charArrayOf('1','1','0','0'),
        charArrayOf('1','0','0','1'),
        charArrayOf('0','0','1','1'),
        charArrayOf('0','0','0','0')
    )

    println("Number of islands: ${numIslands(grid)}")
}

fun numIslands(grid: Array<CharArray>): Int {
    val m = grid.size
    val n = grid[0].size
    var count = 0

    fun dfs(i: Int, j: Int) {
        if (i < 0 || j < 0 || i >= m || j >= n || grid[i][j] == '0') return

        grid[i][j] = '0' // mark visited

        // visit all 4 directions
        dfs(i+1, j)
        dfs(i-1, j)
        dfs(i, j+1)
        dfs(i, j-1)
    }

    for (i in 0 until m) {
        for (j in 0 until n) {
            if (grid[i][j] == '1') {
                count++
                dfs(i, j)
            }
        }
    }

    return count
}


Number of islands: 3
🔍 How it works
Loop through each cell

Agar '1' mile → new island

Count += 1

DFS se poore island ko mark kar do '0' (visited)

Continue looping

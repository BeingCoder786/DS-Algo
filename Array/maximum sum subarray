class Solution {
    fun maxSubArray(nums: IntArray): Int {
   
   var max = Int.MIN_VALUE
   var sum = 0
   for(i in 0 until nums.size){
        
        sum+=nums[i]
        if(max<sum)
           max =sum

           if(sum<0)
            sum =0
   }
   return max
    }
}

class Solution {
    fun containsDuplicate(nums: IntArray): Boolean {
        nums.sort()
        print(nums.toList())
        for(i in 0 until nums.size-1){
            if(nums[i]==nums[i+1])
            return true
        }
        return false
        
    }
}


booleanarray or hashset
val seen = BooleanArray(1001)

for (num in nums) {
    if (seen[num])
        return true

    seen[num] = true
}
